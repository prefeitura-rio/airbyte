# Docker configuration for building and pushing images
DOCKER_IMAGE_NAME=your-dockerhub-username/airbyte-source-typesense-python

# Optional: Override the target platform (default: linux/amd64)
# DOCKER_PLATFORM=linux/amd64,linux/arm64
